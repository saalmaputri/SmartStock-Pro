"""Router package.

The MVP keeps endpoint implementation in app.main for easier presentation.
These modules are provided to match the scalable backend structure and can be
split into APIRouter implementations when the project grows.
"""
